func fib(n: int) {
	if (n == 0) {
		return 0;
	} else if (n == 1) {
		return 1;
	}

	return fib(n - 1) + fib(n - 2);
}

for (var i = 0; i < 20; i = i + 1) {
	print("fib(" + i + ") = " + fib(i: int));
}
